import { FunctionComponent } from "react";
import { GlobalContainer } from "../../styles/global";

interface AdminDashScoreValidationProps {}

const AdminDashScoreValidation: FunctionComponent<
  AdminDashScoreValidationProps
> = () => {
  return (
    <GlobalContainer>
      <p>ADMIN DASH SCORE VALIDATION</p>
    </GlobalContainer>
  );
};

export default AdminDashScoreValidation;
